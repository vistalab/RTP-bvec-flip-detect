from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Neuro detect leverages the possibilities of deep learning to automatically detect and correct formatting errors of MRI scan data.',
    author='Fabian H. Reith / Wandell Lab / Stanford',
    license='MIT',
)
